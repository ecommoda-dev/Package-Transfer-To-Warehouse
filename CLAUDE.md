<div dir="rtl" style="text-align: right;">

# قسم استلام المرتجعات — Package Transfer To Warehouse (`Package-Transfer-To-Warehouse`)

![version](https://img.shields.io/badge/worker-v1.4.0-blue)

**بتعمل إيه:** بتسجّل رجوع الطرد **للمخزن**. الموظف بيسكن باركود الأوردر
المرتجع أو الملغي، والأداة بتكتب `custom.package_whereabouts_s1` (أو `_s2`
لدورة الاستبدال/الاسترجاع) بقيمة **`Warehouse`**.
**مين بيستخدمها:** المخزن
**الإصدار:** `1.4.0` (`WORKER_VERSION` في `index.js`)

> 🔴 **الريبو ده Worker وبس — مفيش واجهة هنا ومفيش نسخة مستقلة** (نفس قرار
> `Package-Transfer-To-Office` بالحرف). الواجهة الوحيدة هي
> **`warehouse-return.html`** جوّه `Warehouse-Operations-Center`.
> ⛔ ممنوع يتضاف `index.html` هنا: نسخة مستقلة معناها مفتاح `localStorage`
> تاني وسر تاني وشاشة دخول تانية — وكلهم بنود مفتوحة في الأدوات اللي
> اتحوّلت للهب، ومفيش سبب نفتحهم من جديد.

> 🔴 **ودي «الأداة التانية» في خطة `package_whereabouts`** — شبكة الأمان
> المسجّلة في `ecommoda-order-lifecycle` → `references/package-whereabouts.md`
> §5، واللي `Package-Transfer-To-Office/CLAUDE.md` سابها بند مفتوح صريح:
> «الطرد اللي عهدته `Office` والأوردر ملغي/مرتجع → سكان يرجّعه `Warehouse`».

## الروابط

```
الـ Worker  : https://package-transfer-to-warehouse-worker.ecommoda-dev.workers.dev
الواجهة     : https://ecommoda-dev.github.io/Warehouse-Operations-Center/warehouse-return.html
tool في D1  : metafields_change           ← 🔴 سجل **مشترك**
extra.sourceTool : package_transfer_to_warehouse   ← ده اللي بيفصل الأداة عن غيرها
مجموعة السر : warehouse_ops             ← 🔴 مش سر فريد للأداة دي
```

## 🔴 الأداة دي معكوس أداة المكتب — والجدول ده أهم حاجة في الملف

الأداتان بيكتبوا على **نفس الحقل** وبيقروا **نفس الميتافيلدات**، والفرق كله
في شرط الأهلية وفي القيمة المكتوبة. أي نسخ حارس من هناك لهنا من غير قلبه
**بيكسر الأداة في صمت**:

| | `Package-Transfer-To-Office` | **دي (`…-To-Warehouse`)** |
|---|---|---|
| الحالة المؤهلة | `S1 = Ready` · `S2 = Ready` و`S1 = Delivered` | **`S1 ∈ {Returned, Cancelled}` · `S2 = Returned`** |
| **الإلغاء** | 🔴 **أول حارس بيرفض** («قطع الشحنة ورجّع المنتجات على الرف») | 🔴 **شرط أهلية** — ده الطرد اللي الأداة اتعملت عشانه |
| العهدة المقبولة **في الطابور** | `Warehouse` أو فاضية | 🔴 **`Office` وبس** (`1.2.0`) |
| العهدة = «خلاص متعمل» | `Office` | **`Warehouse`** |
| العهدة = رفض **في الـ Worker** | `Courier` (رجوع لورا) | **مفيش** — كل عهدة تانية طريق شرعي للرجوع |
| القيمة المكتوبة | `Office` | **`Warehouse`** |
| مصدر الطابور | `manual_status:'Ready'` (حالة **عابرة**) | **تلات استعلامات على حالات نهائية** |
| أرضية تاريخ الأوردر | نافذة متحرّكة `٢٥٠` يوم | **نفس النافذة بالحرف** (`1.4.0`) |
| ترتيب الطابور | الأقدم **تغليفًا** الأول | **نفس الترتيب بالحرف** (كان `updatedAt` لحد `1.0.0`) |

> ⛔ **أخطر نسخ ممكن يحصل: حارس الإلغاء.** لو اتنقل من هناك زي ما هو،
> الأوردر الملغي بيترفض — وده **أكبر شريحة** في الأداة دي. النتيجة أداة
> بتفتح وبتسكن وبترفض كل حاجة تقريبًا، **بلا أي خطأ في الكونسول**. عشان كده
> `S1_ELIGIBLE` ثابت مسمّى في `§CONSTANTS`، والتعليق جنبه بيقول ده بالنص.

## الحقول اللي بتتكتب

| الحقل | إمتى |
|---|---|
| `custom.package_whereabouts_s1` | الشحنة الأصلية (`S1 = Returned` أو `Cancelled`، أو ملغي على شوبيفاي) |
| `custom.package_whereabouts_s2` | دورة الاستبدال/الاسترجاع (`S2 = Returned`) |

القيمة اللي بتتكتب **دايمًا `Warehouse`** (`WA_TARGET` — ثابت مسمّى عشان
مايتكتبش حرفيًا في الكتابة وفي التحقق بعدها).

> 🔴 **النوع لازم يبقى `single_line_text_field` بالحرف** — `metafieldsSet`
> بترفض **الكتابة كلها** لو النوع مش مطابق، والرسالة بتبقى غامضة. عشان كده
> `?action=diag` بيقرا **التعريف الحيّ من شوبيفاي** ويقارن النوع وقايمة
> الاختيار: تغيير التعريف من الأدمن بيبان في ٥ ثواني بدل ما يطلع كفشل كتابة
> على أول سكانة من الموظف.

## شروط الأهلية — ومكانها **اتنين بقرار**

| # | الشرط | ليه |
|---|---|---|
| ① | **مش بوسطة** (المندوب ≠ `Bosta` والزون ≠ `Other_Regions`) | مرتجعات بوسطة ليها أداتها، و`package_whereabouts` مش بيتتبع لطرودها — تحت في §النطاق |
| ② | **الزون قاهرة+جيزة أو شو روم** | نفس نطاق أداة المكتب بالحرف |
| ③ | **مرتجع أو ملغي** — `S1 ∈ {Returned, Cancelled}` أو `cancelledAt` → `_s1` · `S2 = Returned` → `_s2` | ده تعريف «الطرد راجع» |
| ④ | **اتغلّف فعلاً** — وقت تغليف على **ميتافيلد الماكينة بتاعتها** | أوردر اتلغى قبل ما يتغلّف **مفيهوش طرد أصلاً**؛ مفيش حاجة ترجع على الرف |
| ⑤ | **العهدة مش `Warehouse`** | `Warehouse` → «خلاص متعمل» (محايد). ⚠️ **والطابور أضيق من كده من `1.2.0`** — بيعرض `Office` بس، وده **في الـ shell** مش هنا |

> 🔴 **القاعدة واحدة، ومكان تنفيذها اتنين — وده مقصود:**
> **الواجهة** (`wocWarehouseGate` في `shared/shell.js` §WAREHOUSE-GATE) بتقرر
> **إيه اللي يتعرض في الطابور**، و**الـ Worker** (`§SCAN::evaluate`) بيقرر
> **إيه اللي يتكتب** — وهو **المرجع**.
> ⚠️ حارس في الواجهة بس **مش حارس** (`ecommoda-order-lifecycle` §1.5): تاب
> مفتوح من ساعة والحالة اتغيّرت في الوقت ده كان هيعدّي. ودي نفس الحفرة اللي
> أنتجت R3 و§ELIGIBILITY في أداة التغليف.
> ⛔ وأي تعديل على القاعدة **لازم يحصل في المكانين في نفس التمريرة** (درس R1).

> 🔴 **④ أخطر شرط في القايمة — وفشله صامت.** صف S2 بيتفحص بـ
> `custom.s2_packing_date_time` **مش** `s1_…`. الفحص بـ`s1_…` على صف S2
> بيعدّي على طرد **ما اتغلّفش خالص** — مقيس حيًا 15-09-2026 على أداة المكتب:
> ٦ أوردرات `S2 = Ready`، **الستة** عندهم `s1_packing_date_time` و**٢ بس**
> عندهم `s2_…`. يعني الفحص الغلط كان هيعدّي **٦ من ٦**.

## 🔴 النطاق — بوسطة بره، والتحفّظ لازم يتقال

مرتجع بوسطة **بيرجع المخزن فعلاً** فيزيائيًا — بخلاف طرد بوسطة **الخارج**
اللي عمره ما بيعدّي على المكتب. يعني الاستبعاد هنا **أضعف** من نظيره في أداة
المكتب، واللي بيبرّره حاجتان:

- ① `package_whereabouts` **مش بيتتبع لطرود بوسطة أصلاً** في المهارة
  (`ecommoda-order-lifecycle` قاعدة ١٧) — تتبّعها بييجي من بوسطة. كتابة عهدة
  عليها بتخلق محور تتبّع **مالوش كاتب في أول الرحلة**: مفيش حد بيكتب
  `Courier` على طرد بوسطة، فالقيمة هتظهر من العدم عند الرجوع بس.
- ② ومرتجع بوسطة ليه **أداة قايمة**: `bosta-returned.html` (قسم مرتجعات
  بوسطة)، وهي بتلغي الأوردر وبترجّع المخزون فعلاً.

> ⛔ **لو اتقرر يومًا إن بوسطة تدخل النطاق**، التعديل **سطر واحد** في
> `ZONE_IN_SCOPE` هنا و**سطر واحد** في `WOC_WAREHOUSE_ZONES` في الـ shell —
> **في نفس التمريرة** (درس R1)، ومعاه بند في `docs/warehouse-check.mjs`.

## 🔴 ليه الفلترة في الواجهة والـ Worker بيرجّع خام؟

نفس سبب أداة المكتب بالحرف: `custom.package_whereabouts_s1` **مش قابل
للفلترة** في بحث شوبيفاي. الفلتر عليه **بيتجاهَل في صمت** ويرجّع **المتجر
كله**:

```
metafields.custom.package_whereabouts_s1:'Office'  →  10,000 صف (مقيس 15-09-2026)
```

فالـ Worker بيستعلم عن **الحالة بس** وبيرجّع الحقول الخام، والأهلية بتتحسب في
الـ shell.

> ⛔ **ممنوع تحاول تفلتر على الحقل ده في `query`** — الرد هيعدّي بـ`200`
> وبمصفوفة فيها المتجر كله، والطابور هيقول رقم مالوش أي علاقة بالواقع.

## 🔴 أرضية تاريخ الأوردر — رجعت نافذة متحرّكة `٢٥٠` يوم (`1.4.0`)

🔴 **تراجع صريح عن قرار v1.29.0 — قرار أحمد 19-09-2026، §٩ في
`docs/query-cost-experiment.md` بريبو الهب.** الأرضية الثابتة (`2026-04-01`)
اللي كانت هنا **رجعت لنافذة متحرّكة**، بس بعدد أيام أكبر بمراحل من النافذة
القديمة (٣٠ يوم على `updated_at` قبل v1.28.0).

أداة المكتب مصدرها `manual_status:'Ready'`، وهي حالة **عابرة** فعددها عشرات.
هنا المصدر `Returned` و`Cancelled`، وهما حالتان **نهائيتان بتتراكم**: كل
مرتجع وكل ملغي في تاريخ المتجر كله بيطابق الفلتر.

```js
const ROLLING_WINDOW_DAYS = 250;   // قرار أحمد 19-09-2026 (§٩) — نفس القيمة في أداة المكتب
function computeOrdersSince() {
  const ms = Date.now() - ROLLING_WINDOW_DAYS * 24 * 60 * 60 * 1000;
  return new Date(ms).toISOString().slice(0, 10);   // YYYY-MM-DD — UTC
}
```

- 🔴 **القرار اتاخد وهو عارف الثمن بالظبط.** v1.29.0 شالت النافذة المتحرّكة
  للسبب المكتوب وقتها: «بتخفي الطرد الراجع من شهرين **بالظبط لما يبقى منسي
  فعلاً**، وهو الطرد اللي الأداة دي اتعملت عشانه». الثمن ده **لسه صحيح**،
  بس أحمد قرر إنه مقبول مقابل استقرار التكلفة — رفع `STEP1_MAX_PAGES` لوحده
  كان بيتطلّب رفع متكرر كل كام شهر مع نمو المتجر (تفصيل §٩ في الملف
  المرجعي)، والنافذة المتحرّكة بتخلي عدد الأوردرات المطابقة **تقريبًا
  ثابت**.
- 🔴 **الفرق الحاسم عن ٣٠ يوم القديمة: ٢٥٠ يوم (~٨ شهور).** طرد منسي لمدة
  ٨ شهور حالة أندر بكتير من طرد منسي لمدة شهر — مساحة الأمان أكبر بمراحل.
- ⚠️ **شبكة الأمان المطلوبة — بند مفتوح لازم يتنفّذ، مش رفاهية.** الملف
  المرجعي صريح: النافذة المتحرّكة «ممنوع تتنفّذ من غير شبكة أمان مصاحبة» —
  راجع «مسائل مفتوحة» تحت.
- 🔴 **`computeOrdersSince()` بتتحسب لحظيًا في كل نداء** — مش ثابت وقت
  الـ cold start. الـ isolate ممكن يعيش ساعات، والنافذة لازم تفضل «آخر ٢٥٠
  يوم من دلوقتي».
- 🔴 **من غير الأرضية دي أصلاً** الاستعلام بيرجّع آلاف الصفوف، والـ ٣٠٠
  اللي بتعدّي حارس الحلقة بتبقى أحدث حاجة أو أقدم حاجة حسب الترتيب — يعني
  الطابور يقول رقم مالوش أي علاقة بالطرود اللي في إيد الموظف دلوقتي.
- ⚠️ **والفلتر على `created_at` مش `updated_at`** — تاريخ **الأوردر** هو اللي
  الموظف بيقراه في الجدول وبيتصرّف عليه؛ و`updated_at` بيتحرّك مع أي تعديل
  (حتى كتابة الميتافيلد بتاعتنا نفسها)، فكان بيدّي أرضية **بتتزحلق تحت الطرد
  نفسه**. نفس القاعدة اللي منعت الرجوع لـ`updated_at` في v1.29.0 **ما
  اتغيّرتش**.
- ⚠️ **والأرضية على التلات استعلامات** — واحد من غيرها معناه إن حالة بتعرض
  أقدم من التانيتين، والجدول بيخلط النطاقين **بلا أي إشارة**.
- ⚠️ **والنطاق عرض بس — مش شرط أهلية.** الأوردر الأقدم منها **بيتسكن عادي**
  والـ Worker بيكتب عليه؛ هو مش ظاهر في القايمة وبس. ⛔ ممنوع تتحوّل لشرط في
  `evaluate`.
- ⚠️ **والصيغة `YYYY-MM-DD` بتتقرا UTC عند شوبيفاي** — الحساب بـUTC مباشرة
  بلا تحويل توقيت القاهرة، لأن فرق يوم على نافذة ٢٥٠ يوم أثره العملي صفر
  تقريبًا (٠.٤٪ من النافذة) — بخلاف نافذة الـ٣٠ يوم القديمة اللي كان الفرق
  فيها أكبر نسبيًا.
- 🔴 **والأرضية بترجع في الرد (`ordersSince`) والواجهة بتعرضها من هناك** —
  مش مكتوبة بالإيد في الصفحة. تاريخان يفترقوا في صمت هو درس R1 بالحرف، وفيه
  بند في `docs/warehouse-check.mjs` بيقفله.
- ⛔ **ممنوع الرجوع لأرضية ثابتة بقرار من غير مراجعة السبب اللي رجّع النافذة
  المتحرّكة** — نفس المبدأ اللي منع النافذة المتحرّكة الأولى، معكوس.

> 🟡 **بند مفتوح مقصود: الملغي على شوبيفاي نفسها مش في الطابور.** الأوردر
> اللي عليه `cancelledAt` من غير `manual_status = Cancelled` مش بيطابق أي
> من التلات استعلامات. فلتر `status:cancelled` كان هيرجّع **كل** ملغيات
> تاريخ المتجر، فاستُبعد بقرار.
> ⚠️ **والأوردر ده بيتسكن عادي** — `evaluate` بتقبل `cancelledAt` صراحةً.
> يعني هو **مش في القايمة بس السكانة بتشتغل عليه**، وده بالظبط معنى «القايمة
> عرض والـ Worker مرجع».

## الـ Endpoints

| `action` | Method | بيعمل إيه |
|---|---|---|
| `get_config` | GET | `WORKER_VERSION` — الواجهة بتقارنه بـ`min` |
| `diag` | GET | فحص ذاتي بلا كتابة — **مصفوفة `[{ ok, label, detail, hint }]`** |
| `get_employees` | GET | فلتر الموظف في تاب السجل |
| `get_ready_to_warehouse` | GET | المرتجع/الملغي من `2026-04-01` فأحدث بحقوله الخام |
| **`scan`** | **POST** | قراءة الأوردر حيًّا + الحكم + `metafieldsSet` + صف D1 |
| `get_logs` · `get_logs_count` · `get_logs_export` | GET | تاب السجل |

> 🔴 **مفيش `check_employee`/`register_pin`/`verify_employee`/`log_logout`
> — انحراف مقصود.** الأداة مالهاش واجهة مستقلة، والدخول بيحصل **مرة واحدة**
> في `index.html` بتاع الهب عبر Worker التغليف. نقطة دخول تانية هنا = سطح
> هجوم بلا مستهلك. نفس قرار أداة المكتب و`order-sku-barcode-printer-worker`.
> ⚠️ و`get_employees` **موجود** لأنه مستهلَك فعلاً — من غيره فلتر الموظف في
> السجل بيفضل فاضي **بلا أي خطأ**.

### عقد `scan`

```jsonc
// الطلب
{ "code": "7212244533570", "ack": false, "employee": "ahmed" }

// الرد — ست نتايج
{ "result": "success"   }  // اتكتبت واتأكدت من رد شوبيفاي
{ "result": "warning"   }  // اتكتبت، وكان فيه حاجة الموظف أقرّ بيها
{ "result": "error"     }  // النداء وصل لشوبيفاي واترفض — ما اتكتبتش
{ "result": "rejected"  }  // اتوقف قبل أي محاولة — بالسبب
{ "result": "already"   }  // العهدة `Warehouse` أصلاً — **محايدة مش فشل**
{ "result": "needs_ack" }  // حالة شاذة — مفيش كتابة ومفيش صف لحد الإقرار
```

> 🔴 **قاعدة عدم الضياع:** كل سكانة بتسيب **صف واحد بالظبط** في D1 — يا
> `update` يا `rejected`. صفر صفوف = سؤال «الأوردر ده اتسكن ولا لأ؟» مالوش
> إجابة بعد ما الشاشة تتقفل.
> ⚠️ **الاستثناء الوحيد `needs_ack`** — مفيش قرار اتاخد لسه، والسكانة
> **هتكمّل** بصف كامل بعد الإقرار. صف هنا كان هيبقى صفّين لسكانة واحدة.

> 🔴 **`already` ممنوع تتعدّ فشل وممنوع تاخد لون تحذير** (`ecommoda-constants`
> §12). في الأدوات الشقيقة دي كانت **٥٩ من ٦١** صف فشل و**٦ من ٧** صف رفض —
> يعني أكتر نتيجة بتتكرر، ولو اتلوّنت أحمر الموظف بيتعلّم يعدّي على الأحمر كله.

### الحالات الشاذة — بتكمّل بإقرار مش بتتمنع

- الشحنة الأصلية ودورة الاستبدال **الاتنين** مرتجعين/ملغيين → بيتسجّل على
  **S1** بإقرار (الموظف يسأل لو الطرد اللي في إيده بتاع الاستبدال).
- `S2 = Returned` والشحنة الأصلية لسه مش `Delivered` → تسلسل غير متوقّع.
  **مش رفض** — الطرد في إيد الموظف فعلاً — بس لازم يتقال.
- عهدة الطرد فيها قيمة بره قايمة الاختيار → Rule 13: **بلّغ عنها،
  متحركهاش في صمت**.
- حالة S1 أو S2 بره القايمة المعتمدة.

> ⚠️ **ومفيش صف في D1 قبل الإقرار** — إقرار ما اتسألش عنه ومكتوب في السجل =
> كذب في السجل (نفس قاعدة `bostaUpdated` في أداة الطباعة و`eligibilityAck`
> في التغليف). و`extra.acknowledged` بيتبعت **بس لما الإقرار يحصل فعلاً**.

### فخّ البحث بالاسم

الرقم **١٠ خانات فأكتر** = ID رقمي → **قراءة بمفتاح** (`order(id:)`).
أقل من كده = اسم أوردر → **بحث** (`orders(query:"name:#54727")`).

> 🔴 **البحث بالاسم بحث مش قراءة** — `name:#5362` بيطابق `#53621` كمان.
> المطابقة التامة على الاسم الراجع **إلزامية**، ومن غيرها الأداة بتكتب
> `Warehouse` على **أوردر تاني** (`shopify-graphql-helper` §name-search).

## D1

```
tool             : metafields_change                ← سجل مشترك بين أربع أدوات
extra.sourceTool : package_transfer_to_warehouse    ← الفاصل الوحيد بين الكُتّاب
type             : update · rejected
```

| `type` | `extra.result` | معناه |
|---|---|---|
| `update` | `success` · `warning` · `error` | اتبعت لشوبيفاي — نجح أو اترفض |
| `rejected` | `rejected` · `already` | اتوقف **قبل** أي محاولة |

> ✅ **صفر قيمة `type` جديدة** — `update` و`rejected` **مسجّلتين أصلاً** لصف
> `metafields_change` في `ecommoda-constants` §7، فـRule 7 مالهاش نطاق جديد
> هنا. اللي **لسه مطلوب** هو تسجيل الأداة في **جدول الكُتّاب** (§7 بيقول
> صراحةً: أي أداة جديدة بتكتب تحت `metafields_change` **تسجّل نفسها**).

مفاتيح `extra`: `result` · `stage` (`lookup`/`write`) · `machine` (`S1`/`S2`) ·
`zone` · `courier` · `s1` · `s2` · `cancelledAt` · `packedAt` · `packedBy` ·
`code` · `scanned` · `acknowledged` · `warnings[]` · `cairoDate`.

و`value_before`/`value_after` = **عهدة الطرد** قبل وبعد (`Office` →
`Warehouse`).

> 🔴 **الدخول/الخروج مش هنا** — بيتسجّلوا تحت `warehouse_ops_center` من
> Worker التغليف (الهب هو اللي بيسجّل دخوله). الأداة دي **مابتكتبش**
> `login`/`logout` خالص.

> 🔴 **استعلام خط الأساس لازم يفلتر على `extra.sourceTool` و`extra.result`** —
> الاستعلام اللي بيعدّ `type` بس **بيقيس المحاولات مش الكتابة الفعلية**،
> و**بيخلط أربع أدوات**:
> ```sql
> SELECT COUNT(*) FROM logs WHERE tool = 'metafields_change' AND json_extract(extra,'$.sourceTool') = 'package_transfer_to_warehouse' AND type = 'update' AND json_extract(extra,'$.result') = 'success';
> ```

> 🔴 **والثمن لازم يتقال:** الصف ده كمان هو المصدر اللي
> `ecommoda-order-lifecycle` قاعدة ٩ بتسمّيه لتاريخ **حالة الأوردر**. والأداة
> دي **مابتلمسش `manual_status` ولا `status_2_r_e` خالص** — بتكتب **عهدة
> الطرد**، محور تاني تمامًا. يعني أي استعلام ساذج بيعدّ
> `tool='metafields_change' AND type='update'` بقى بيضخّم انتقالات الحالة
> بصفوف عهدة — **من أربع كُتّاب دلوقتي بدل تلاتة**.

> 🔴 **انحراف موثّق عن `ecommoda-constants` §7 — مفتاح `sourceTool` مش
> `source`.** §7 بتقول: «أي أداة جديدة تكتب تحت `metafields_change` تستخدم
> **`extra.source`**». الانحراف هنا **موروث بقرار** من أداة المكتب وسكانرَي
> بوسطة — عشان يفضل مفتاح **واحد** يفصل كُتّاب نفس الصف بدل اتنين.
> ⚠️ **ولازم يتسجّل مع تسجيل الأداة في جدول الكُتّاب** — وإلا أول جرد بيتكتب
> على `extra.source` بيرجّع **صفر صف** للأداة دي، بلا أي خطأ.

## CORS

**Option B — قايمة صارمة.** الأداة **بتكتب على شوبيفاي**، فمفيش wildcard:

```js
const ALLOWED_ORIGINS = ['https://ecommoda-dev.github.io'];
```

## النشر — الخطوات اليدوية (كلها حاجزة)

| # | الخطوة | من غيرها |
|---|---|---|
| ① | إنشاء الـ Worker باسم **`package-transfer-to-warehouse-worker`** بالظبط | الرابط في `WOC_WORKERS.warehouse.url` مايردّش |
| ② | ربط Workers Builds على الريبو ده (`main`) | أي push مابينشرش |
| ③ | **`WORKER_SECRET` = قيمة مجموعة `warehouse_ops`** → **Promote** | كل نداء `401` |
| ④ | `CLIENT_ID` + `CLIENT_SECRET` → **Promote** | OAuth بيفشل والطابور بيقع |
| ⑤ | تسجيل الأداة في **جدول كُتّاب `metafields_change`** في `ecommoda-constants` (§7 · `references/d1-history.md`) — ومعاه انحراف `sourceTool` | جرد بيعدّ الصف ده كأداة واحدة **بيغلط**، وجرد بيدوّر على `extra.source` **بيرجّع صفر** |

> 🔴 **③ أهم واحدة فيهم، والفخّ إن `WORKER_SECRET` هنا مش قيمة فريدة.** الهب
> بيبعت **سر واحد** لكل الـ Workers من مفتاح `localStorage` واحد
> (`warehouse_ops_worker_secret` · Standards #39). سر خاص بالأداة = `401` على
> كل نداء، والصفحة بتفتح والهيدر شغّال وأول استعلام بيقع.
> ⚠️ **والأداة دي مالهاش نسخة مستقلة**، فمفيش سر قديم لازم يتلزق في مكانين.
> ⚠️ `?action=diag` بيثبّت الحاجة دي: بند «متغيرات وأسرار الـ Worker».

> ⚠️ **`[vars]` في `wrangler.toml` بس** — ممنوع تضيف `SHOP_DOMAIN` بإيدك في
> الداشبورد لأداة منشورة من git: أول push بيدهس القيمة اليدوية (واقعة موثّقة
> على `stylebox-price-sync-worker` · 12-09-2026).

## فحص المتصفح

الفحص بتاع الأداة دي في ريبو الهب (الواجهة عايشة هناك):

```bash
cd Warehouse-Operations-Center
npm i playwright postcss --no-save
node docs/warehouse-check.mjs      # ٨٣ بند
```

> 🔴 **أربع عيلات فشل صامتة الملف ده اتكتب عشانها**، ومستحيل يتمسكوا بمراجعة
> كود:
> ① شرط أهلية اتكتب في الصفحة بدل الـ shell → الطابور والرئيسية يقولوا رقمين
>   مختلفين **وصفر خطأ في الكونسول** (درس R1 · v1.11.0).
> ② **حارس الإلغاء اتنسخ من أداة المكتب** → الملغي بيترفض، والأداة بترفض
>   أكبر شريحة عندها **بلا أي رسالة تقول ليه**.
> ③ صف S2 بيتفحص بوقت تغليف S1 → طرد ما اتغلّفش بيعدّي **بلا أي رسالة**.
> ④ الصف مابيتشالش من الطابور بعد النجاح → الشاشة زي ما هي فالموظف بيسكن
>   تاني (٥٩ صف فشل كذّاب في سكانر المرتجعات).

## مسائل مفتوحة

- 🔴 **الأربع خطوات النشر اليدوية فوق — كلها حاجزة** (إنشاء الـ Worker ·
  Builds · السر · OAuth).
- 🔴 **تسجيل الأداة ككاتب جديد في `metafields_change`** (`ecommoda-constants`
  §7 · `references/d1-history.md`) — **مش صف `tool` جديد**: `update` و
  `rejected` مسجّلتين أصلاً، فصفر قيمة `type` جديدة. اللي مطلوب: الأداة
  تتسجّل في **جدول الكُتّاب** بقيمة
  `extra.sourceTool = package_transfer_to_warehouse`، ومعاها **انحراف
  `sourceTool` بدل `source`**.
  ⚠️ من غير ده أي جرد بيعدّ `metafields_change` كأداة واحدة **بيغلط**، وأي
  جرد بيدوّر على `extra.source` **بيرجّع صفر صف** للأداة دي — **بلا أي خطأ**.
- 🔴 **تحديث `ecommoda-order-lifecycle` → `package-whereabouts.md` §5** —
  المهارة كانت مسجّلة «الأداة رقم ٣» على إنها **أداة واحدة بقايمتين**، وقرار
  أحمد 15-09-2026 إنها **أداتين**. الأداتان بقوا موجودين دلوقتي (المكتب
  15-09-2026 · دي 19-09-2026)، فالبند بقى **تحديث توثيق مش تخطيط**.
  ✅ و**G-17 في `known-gaps.md` بقى ليه أداة** — «الطرد المنسي في المكتب»
  كان بند مفتوح لأن `Office` مالهاش طريق رجوع؛ الطريق ده بقى موجود.
  ⚠️ **بس البند مايتقفلش قبل التشغيل الحيّ** — أداة موجودة ومش مستخدمة
  مابتقفلش فجوة.
- 🟡 **أداة التغليف لسه ما بتكتبش `Warehouse` وقت التغليف** — هي «الأداة رقم
  ١» في خطة المهارة. ⚠️ **والأثر هنا معاكس لأثره في أداة المكتب:** هناك
  الفاضي **لازم يتقبل** وإلا الطابور بيفتح فاضي؛ هنا الفاضي بيتقبل كمان
  (الطرد راجع فعلاً)، بس **معناه أضعف**: إحنا مش عارفين هو كان فين.
  ✅ والأداة دي بتصلّح الفاضي ده بنفسها — أول سكانة بتكتب `Warehouse` صراحةً.
- 🟡 **بوسطة بره النطاق** — التحفّظ الكامل فوق في §النطاق. القرار لأحمد،
  والتغيير سطرين (هنا وفي الـ shell) في نفس التمريرة.
- 🟡 **الملغي على شوبيفاي نفسها مش في الطابور** — فوق في §النافذة الزمنية.
  **مش حاجز**: السكانة بتشتغل عليه عادي.
- 🔴 **الأرضية الثابتة اتلغت ورجعت نافذة متحرّكة `٢٥٠` يوم (`1.4.0` · قرار
  أحمد 19-09-2026 §٩)** — راجع القسم فوق للتفاصيل الكاملة والمبرّر.
- 🔴 **شبكة الأمان المطلوبة مع النافذة المتحرّكة — بند مفتوح لازم يتنفّذ.**
  `docs/query-cost-experiment.md` §٩ صريحة: نافذة متحركة «ممنوع تتنفّذ من
  غير شبكة أمان مصاحبة». المطلوب حاليًا: **تقرير دوري يدوي** (مرة كل شهر)
  بيراجع الأوردرات `Returned`/`Cancelled` الأقدم من `computeOrdersSince()`
  الحالية — عشان طرد نسي أكتر من ٢٥٠ يوم مايفضلش **بلا أي متابعة خالص**.
  الحل الدائم (فهرس D1 لعهدة الطرد — خيار ج في §٩) **مؤجّل** لحد ما المشكلة
  ترجع أو مجهوده يتاح. لحد ما التقرير الدوري يتظبط، الأرضية دي بترجع لنفس
  ثمن v1.28.0 القديم (٣٠ يوم) بس بمساحة أمان أكبر (٢٥٠ يوم).
- 🟡 **`read_all_orders` مش من صلاحيات التطبيق** — موروث من الستاك، و**الأداة
  دي أكتر واحدة معرّضة**: بتقرا أوردرات مرتجعة وملغية، وهي أقدم بطبيعتها من
  الجاهزة للشحن. شوبيفاي بتخفي أي أوردر أقدم من **٦٠ يوم** عن تطبيق مالوش
  الصلاحية، والرسالة اللي بتطلع «الأوردر مش موجود على شوبيفاي» — وهي **كاذبة
  تمامًا**. بند `?action=diag` بيقولها بالاسم. القرار لأحمد.
- 🟢 **الحقل مش بيبوّب أي فعل** — `package_whereabouts` مابيمنعش طباعة ولا
  شحن ولا إلغاء. السؤال ده مفتوح في المهارة (§7) ومحدش طلبه.

## 🔴 `order.updatedAt` اتشال بالكامل (`1.1.0` · قرار أحمد 19-09-2026)

النسخة `1.0.0` كانت بتجيب `updatedAt` وبترتّب بيه، والواجهة كانت بتعرضه في
عمودين («آخر تحديث» · «الوقت منذ آخر تحديث»). الحقل **اتشال من الاستعلام ومن
شكل الرد ومن الترتيب**، والعمودان اتشالوا من الصفحة.

- 🔴 **السبب: كان تقريب بيتقري تسجيل.** شوبيفاي **مالهاش حقل** بيقول «الطرد
  رجع إمتى»، و`updatedAt` بيتحرّك مع **أي** تعديل على الأوردر — تاج · تعديل
  بند · وحتى **كتابة الميتافيلد بتاعتنا إحنا**. الدليل الحيّ: عملية جماعية
  واحدة خلّت صفوف كتير تقول **نفس التاريخ بالظبط**، والموظف بيقراها «كلهم
  رجعوا في اليوم ده».
- 🔴 **والترتيب بقى بوقت التغليف** — نفس `Package-Transfer-To-Office` بالحرف.
  وقت التغليف **مش مثالي هنا** (الطرد ممكن يكون اتغلّف من شهر ورجع من ساعة)،
  بس هو **وقت مسجّل فعلاً** بدل تقريب بيتحرّك لوحده.
- ⚠️ **و`sortKey` في `QUEUE_QUERY` بقى `CREATED_AT`** زي أداة المكتب — متسق
  مع أرضية `created_at`، ومفيش أثر تاني لـ`updatedAt` في الملف.
- ⛔ **ممنوع يرجع** — لا كحقل ولا كترتيب ولا كعمود — إلا لما يبقى فيه **تسجيل
  حقيقي** لوقت الرجوع (ميتافيلد الأداة دي بتكتبه وقت السكانة). وفيه بندان في
  `docs/warehouse-check.mjs` بيقفلوا الرجوع.
- ✅ **و`warehouse.min` في الـ shell فضل `1.0.0`** — الواجهة بقت معتمدة على
  **أقل** مش أكتر، فالتدهور آمن بالكامل: على `1.0.0` الحقل بيرجع ومحدش بيقراه.
  ترفيع `min` هنا كان هيولّع تحذير كاذب بلا سبب (Standards #29).

## 🔴 سقف الاستعلام اترفع لـ١٬٠٠٠ (`1.2.0` · قرار أحمد 19-09-2026)

```js
const QUEUE_PAGE_SIZE = 50;
const QUEUE_MAX_PAGES = 20;   // كان 6 — يعني 300
```

- 🔴 **الرقم ٦ اتنسخ من أداة المكتب، والنسخ ده كان غلط.** هناك المصدر
  `manual_status:'Ready'` وهي حالة **عابرة** فعددها عشرات؛ هنا المصدر
  `Returned` و`Cancelled`، وهما **نهائيتان بتتراكم** — كل مرتجع وكل ملغي من
  الأرضية لحد النهاردة بيطابق. فالسقف كان بيتضرب **من أول يوم**: الطابور
  بيقول ٢٠٤ والحقيقة أكتر، وبانر الاقتطاع بيقول إن الرقم ناقص.
- 🔴 **والرفع بقى ألزم مع فلتر `Office`** — الفلترة على العهدة بتحصل **في
  الواجهة** (الحقل مش قابل للفلترة في بحث شوبيفاي)، فالـ Worker لازم يجيب
  صفوف خام كفاية عشان الـ`Office` يطلعوا من وسطها.
- ⚠️ **والثمن المعلن:** زمن الجلب الأول أطول (لحد ٢٠ نداء متتالي لكل استعلام
  × تلات استعلامات بالتوازي)، وتكلفة استعلام أعلى على شوبيفاي.
- ⛔ **ومتطلّعش `QUEUE_PAGE_SIZE` لـ100** عشان تقلّل النداءات من غير ما تقرا
  `actualQueryCost` — الاستعلام بـ١١ ميتافيلد لكل نود.
- ⚠️ **وهو لسه حارس حلقة مش سقف بيانات** — لو اتضرب، `truncated` بيرجع
  والبانر بيظهر. ⛔ ممنوع يتشال خالص.

## 🔴 §QUEUE بقت تمريرتين مش واحدة (`1.3.0` · تجربة — قرار أحمد 19-09-2026)

سقف `١٬٠٠٠` فوق (`1.2.0`) **حصلت فعلاً عليه `THROTTLED` حي** على الشاشة
الرئيسية للهب (سكرين شوت — صف «جاهز للرجوع للمخزن» طلّع `تعذّر` بالنص
`returnedOrders: Throttled [THROTTLED]`). السبب الجذري مش رقم الصفحة نفسه —
هو إن الاستعلام كان بيجيب **كل** الحقول التقيلة (زون · مندوب · وقتَي تغليف ·
عهدتَي طرد — ١١ ميتافيلد) لكل أوردر **مطابق للحالة بس** (`Returned`/
`Cancelled`)، بينما شرط العهدة (`Office`) بيستبعد الأغلبية **بعد** الجلب في
الواجهة. التكلفة كانت بتتدفع كاملة على أوردرات هتترمى في الآخر.

> 🔴 **التجربة دي جزء من مراجعة أوسع على الخمس أدوات كلها (`print` · `pack` ·
> `bosta-shipped` · `office-transfer` · `warehouse-return`) — راجع
> `docs/query-cost-experiment.md` بريبو `Warehouse-Operations-Center`.**
> اتقرر نبدأ بالأداة دي بس لأنها **الوحيدة اللي عندها `THROTTLED` مسجّل
> فعليًا** وأكبر مصدر متراكم (`Returned`+`Cancelled` مش حالة عابرة). باقي
> الأدوات متسابة زي ما هي لحد ما نشوف نتيجة التجربة دي.

**التصميم الجديد:**

```js
const STEP1_PAGE_SIZE = 250;   // تمريرة ١ — ٦ حقول بس (id/name/cancelledAt/w1/w2)
const STEP1_MAX_PAGES = 4;     // ٢٥٠×٤ = ١٬٠٠٠ — نفس سقف البيانات القديم
const STEP2_CHUNK     = 50;    // تمريرة ٢ — كامل ORDER_FIELDS، نفس رقم v1.2.0
const STEP2_MAX_IDS   = 3000;  // حارس دفاعي على عدد المرشّحين
```

- 🔴 **تمريرة ١ رخيصة:** نفس التلات استعلامات القديمة (`manual_status:Returned`
  · `manual_status:Cancelled` · `status_2_r_e:Returned`)، بس بحقول أقل بكتير —
  `id`/`legacyResourceId`/`name`/`cancelledAt`/`w1`/`w2` بس. أرخص بمراحل من
  الاستعلام الكامل، فسقف الصفحة رجع لـ`٢٥٠` (نفس رتبة `print.html`/`pack.html`).
- 🔴 **فلترة العهدة (`Office`) بقت في الـ Worker** — `mergeStep1()` بتطبّق
  **نفس أولوية `wocWarehouseGate`** بالحرف (S1 أول لو الحالة طابقت أو
  `cancelledAt` موجودة، وإلا S2)، وبترجّع مجموعة IDs المرشّحين بس.
  ⛔ **وده مش نقل الأهلية للـ Worker** — باقي الشروط (الزون · المندوب · وقت
  التغليف بميتافيلد الماكينة الصح) لسه بالكامل في `wocWarehouseGate`، زي ما
  هي. الـ Worker بيفلتر بند واحد بس (العهدة) عشان يقلّل حجم البيانات التقيلة
  المطلوب جلبها.
- 🔴 **تمريرة ٢ — `nodes(ids:)` بكامل `ORDER_FIELDS` للمرشّحين بس.** على
  دفعات `STEP2_CHUNK=50` (نفس الرقم المقاس آمن في `1.2.0` على نفس الحقول
  بالظبط).
- ⚠️ **الرد للواجهة زي ما هو بالظبط** — صفر تغيير في العقد (`shapeOrder`
  نفسها، نفس المفاتيح). الفرق الوحيد: عدد الصفوف أقل، لأن اللي اتشال كان
  هيترمى في `wocWarehouseQueue` أصلاً (تحقّق: `warehouse-return.html` و
  `index.html` بيطبّقوا الفلترة فورًا على `data.orders` بلا استخدام تاني
  للمصفوفة الخام). ⛔ **صفر رفع لـ `warehouse.min` في الـ shell** — العقد ما
  اتغيّرش.
- 🟡 **`queryCost` حقل تجريبي جديد في الرد** — `{ step1Calls, step1ActualTotal,
  step2Calls, step2ActualTotal, totalActual, candidatesAfterStep1,
  detailsFetched }`، مبني على `data.extensions.cost.actualQueryCost` الحقيقي
  من شوبيفاي (مش `throttleStatus` بس). للقياس بس — الواجهة مش محتاجة تقراه.
- ⚠️ **`?action=diag` بقى فيه بند تاني** («تكلفة آخر نداء (actualQueryCost)»)
  جنب بند `throttleStatus` القديم.

⛔ **وده مش حل لمشكلة إن `package_whereabouts_s1`/`_s2` مش Admin filterable —
القيد نفسه لسه موجود.** التمريرتين دول تخفيف لتكلفة القيد، مش إلغاء له.

🟡 **بند مفتوح جديد — تفعيل Filtering على تعريف الميتافيلد نفسه.** شوبيفاي
فيها خيار "Filter on the order list and in the Admin API" على مستوى تعريف
الميتافيلد (Settings → Custom data → Orders → الحقل → Options) — لو اتفعّل
على `package_whereabouts_s1`/`_s2`، فلتر `query` المباشر ممكن يشتغل فعليًا
من غير أي حاجة من التمريرتين دول. **بس الخيار ده مورد نادر ومشترك على مستوى
المتجر كله: محدود بـ٥ تعريفات فلترة بس** (سكرين شوت من شوبيفاي — راجع
`docs/query-cost-experiment.md`). القرار لأحمد، والتفاصيل الكاملة هناك.

## بصمة المهارات

| المهارة | الإصدار وقت آخر تعديل |
|---|---|
| ecommoda-worker-builder | **v3.7.0** |
| ecommoda-constants | **v3.1.0** |
| ecommoda-order-lifecycle | **v1.8.0** |
| shopify-graphql-helper | **v1.1.0** |
| ecommoda-html-builder | **v6.6.0** |

آخر مطابقة: 24-09-2026 · الـ Worker `1.4.1`
🔴 معلّقة: **إنشاء الـ Worker + ربط Builds + `WORKER_SECRET` (سر `warehouse_ops`)
+ `CLIENT_ID`/`CLIENT_SECRET` → Promote** · **تسجيل الأداة ككاتب جديد على
`metafields_change` في `ecommoda-constants` §7 (بقيمة `extra.sourceTool`)** ·
تحديث `ecommoda-order-lifecycle` (`package-whereabouts.md` §5 · `known-gaps.md` G-17) ·
**شبكة الأمان (تقرير دوري) للنافذة المتحرّكة الجديدة — راجع «مسائل مفتوحة»**

---

آخر تحديث: 24-09-2026 — `1.4.1` (🔴 **الحارس الديناميكي لقيم اللوج (الطبقة ٥
· `ecommoda-worker-builder` Step 7-ج).** `check-log-values.mjs` اتستبدل
بنسخة مصلَّحة بتمسك `{ tool, type }` object shorthand كمان (مش `type:` بنقطتين
بس) — التشيك القديم كان بيعدّي عليه في صمت. القياس على الكود الحالي: صفر
قيم ديناميكية وصفر قيم ناقصة، `log-values.json` مطابق للكود ١٠٠٪
(`update` · `rejected`). `LOG_REGISTRY` (`§LOG-REG` في `index.js`) اتبنى
من نفس الملف، ومفتاحه الزوج `(tool, type)`. الحارس اتحط جوّه `writeLog`
نفسها (الأنكور الوحيد في الريبو ده — مفيش `writeLogsBatch`): قيمة غير
مسجّلة بتتكتب عادي + `extra._unregistered = true` + UPSERT صامت في
`log_value_alerts`. **مفيش رفض كتابة أبدًا.** صفر تغيير في المنطق
التشغيلي أو شكل `log-values.json` — مراقبة بس)

19-09-2026 — `1.4.0` (🔴 **§٨+§٩ من `docs/query-cost-experiment.md`
— قرار أحمد النهائي.** §٨②: `getAccessToken()` بقت بنفس انضباط `shopifyGQL`
(retry + backoff ٣ محاولات) — تصحيح عام للخمس Workers المشتركة في نفس الـ
Custom App، مش خاص بالأداة دي. §٩: `STEP1_MAX_PAGES` اترفع من `٤٠` لـ**`١٠٠`**
(٢٥٬٠٠٠ صف لكل استعلام) **مع** رجوع النافذة الزمنية المتحرّكة (`ROLLING_
WINDOW_DAYS = 250` بدل أرضية `2026-04-01` الثابتة) — قرار أحمد الصريح: خيار
أ + ب مع بعض من §٩، مش فهرس D1 (خيار ج، التوصية الأساسية) اللي اتأجّل لمرحلة
تانية. ⚠️ **وده تراجع مُعلَن عن قرار v1.29.0** اللي شال النافذة المتحرّكة
بالظبط للسبب ده — راجع قسم «أرضية تاريخ الأوردر» فوق للمبرّر الكامل والثمن
المُعلَن. `ordersSince` في الرد و`?action=diag` بيتحسبوا لحظيًا من
`computeOrdersSince()` مش ثابت. صفر تغيير في عقد الرد للواجهة، صفر رفع لـ
`warehouse.min`)

19-09-2026 — `1.3.2` (🟡 **تجربة، تعديل قياس حي تاني** —
القياس بعد الرفع لـ`٢٠` صفحة رجّع `totalActual: 1116` (عدّى ميزانية
الأمان ٧٠٠، بس نجح — الرصيد بيترمّم أثناء الـ٣٤ نداء المتتالية) و`s1Cancelled`
**لسه بتتقصّ** عند ٢٠ صفحة (`step1Detail` كشف السبب بالاسم لأول مرة).
`STEP1_MAX_PAGES` اترفع للضعف (`٢٠` → `٤٠` = ١٠٬٠٠٠ صف) بقرار أحمد.
⛔ **وقرار منفصل: تفعيل Admin Filtering على `package_whereabouts` اتقفل
نهائيًا** — الخمس سلوتات المتاحة على مستوى المتجر مشغولة، والبند ده مش
موضوع نقاش تاني. ⚠️ **وتحذير مسجّل في الكود:** لو الرفع ده لسه مش كفاية،
الحل مش رفع رقم صفحات تالت مرة — المشكلة الجذرية تضييق نطاق `ORDERS_SINCE`
نفسه، قرار منفصل مش سقف تقني)

19-09-2026 — `1.3.1` (🟡 **تجربة، تعديل قياس حي** — أول نشر
لـ`1.3.0` رجّع `truncated: true` فعليًا: `step1Calls: 11` بتكلفة
`step1ActualTotal: 354` بس (~٣٢ نقطة/نداء)، يعني واحد على الأقل من التلات
استعلامات تعدّى ١٬٠٠٠ أوردر مطابق من غير ما التكلفة تقترب من الخطر. `STEP1_MAX_PAGES`
اترفع من ٤ لـ**٢٠** (نفس رقم `QUEUE_MAX_PAGES` القديم، بس على استعلام أخف
بمراحل من اللي كان مطبَّق عليه) · ومعاه `queryCost.step1Detail` — تفصيل
`{op, pages, truncated}` لكل واحد من التلات استعلامات لوحده، عشان أي
`truncated` جاي بعد كده يتحدّد سببه فورًا مش يتخمّن)

19-09-2026 — `1.3.0` (🟡 **تجربة** — §QUEUE بقت تمريرتين مش واحدة
بعد `THROTTLED` حي على `1.2.0`: تمريرة رخيصة تحدد المرشّحين بفلتر العهدة
`Office` (بنفس أولوية `wocWarehouseGate`)، وتمريرة `nodes(ids:)` تجيب
التفاصيل الكاملة للمرشّحين بس · `actualQueryCost` بقى متسجّل ومعروض في
`diag` والرد نفسه (`queryCost`) · صفر تغيير في عقد الرد · صفر رفع لـ
`warehouse.min`)

19-09-2026 — `1.2.0` (🔴 سقف الاستعلام من ٣٠٠ لـ**١٬٠٠٠** لكل
استعلام — الرقم القديم اتنسخ من أداة حالتها **عابرة**، وحالات الأداة دي
**بتتراكم** فالسقف كان بيتضرب من أول يوم)

19-09-2026 — `1.1.0` (🔴 `order.updatedAt` اتشال بالكامل — من
الاستعلام ومن الرد ومن الترتيب · `sortKey` بقى `CREATED_AT` · والترتيب بقى
بالأقدم تغليفًا زي أداة المكتب)

19-09-2026 — `1.0.0` (أول إصدار)

</div>
